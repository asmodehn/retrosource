defmodule RetroSource do
  @doc "builds a source from a stream, using it as an infinitely reusable generator"
  def reusable_source(stream) do
    :not_yet_implemented
  end

  @doc "builds a source from a stream, using it as a generator, only once."
  def consumable_source(stream) do
    :not_yet_implemented
  end
end
